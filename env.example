# =============================================================================
# FitLife - Variáveis de Ambiente
# =============================================================================
# 
# IMPORTANTE: Este é apenas um arquivo de exemplo!
# 
# Os scripts start.sh e start.bat criarão automaticamente um arquivo .env
# com valores apropriados quando você executar o projeto pela primeira vez.
#
# Se você precisar configurar manualmente:
# 1. Copie este arquivo para .env
# 2. Atualize os valores conforme necessário
# =============================================================================

# Network IP - usado pelo Expo para exibir o endereço correto do Metro bundler
# Será detectado automaticamente pelos scripts de inicialização
REACT_NATIVE_PACKAGER_HOSTNAME=192.168.1.100

# JWT Secrets - usados para geração e validação de tokens
# CRÍTICO: Estes valores DEVEM ser únicos e secretos em produção!
# Os scripts de inicialização geram automaticamente valores aleatórios seguros
JWT_SECRET=your_jwt_secret_here_minimum_32_characters_long
JWT_REFRESH_SECRET=your_jwt_refresh_secret_here_minimum_32_characters_long

# SendGrid Configuration (opcional - para notificações por email)
# Necessário apenas se você quiser enviar emails (recuperação de senha, etc.)
# SENDGRID_API_KEY=SG.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# SENDGRID_FROM_EMAIL=noreply@fitlife.com
# SENDGRID_FROM_NAME=FitLife

# =============================================================================
# Notas Importantes:
# =============================================================================
# 
# 1. NUNCA commite o arquivo .env no Git (já está no .gitignore)
# 2. Em produção, use variáveis de ambiente reais do seu servidor/cloud
# 3. Os JWT secrets devem ter pelo menos 32 caracteres
# 4. Troque TODOS os valores padrão antes de fazer deploy em produção
# 5. Mantenha backups seguros dos seus secrets de produção
# 
# =============================================================================
